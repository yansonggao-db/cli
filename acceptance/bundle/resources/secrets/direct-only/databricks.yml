bundle:
  name: test-bundle

variables:
  secret_value:
    description: "Test secret value"
    default: "test-value"

resources:
  secrets:
    secret1:
      catalog_name: main
      schema_name: default
      name: test_secret
      value: ${var.secret_value}
